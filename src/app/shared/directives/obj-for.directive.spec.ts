import { ObjForDirective } from './obj-for.directive';

describe('ObjForDirective', () => {
  it('should create an instance', () => {
    const directive = new ObjForDirective();
    expect(directive).toBeTruthy();
  });
});
